import "./App.css";

function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="label">Demo Scaffold</p>
        <h1>Continual Process Automation Copilot</h1>
        <p>
          Vite + React + TypeScript setup is ready. The next steps will add retrieval, continual
          learning, and optional WebLLM planning.
        </p>
      </section>
    </main>
  );
}

export default App;
