# CasePath convergence defect ledger

Statuses: `open`, `in_progress`, `fixed_unverified`, `closed`, or
`external_dependency`.

| ID | Severity | Symptom and reproduction | Root cause / subsystem | Fix and regression evidence | Status |
|---|---|---|---|---|---|
| CP-001 | Critical | POST a flagship review with `decision=reject` and `building_envelope_mode=required_now`; response still approves v4 and changes the later claim to the opposite evidence state. | Review decision is recorded but ignored; promotion fields are hard-coded in `pipeline_v15.py`. | Typed request enums and decision branching now prevent rejection from creating memory/candidates or changing knowledge; aligned backend rejection and duplicate-review tests pass. | closed |
| CP-002 | Critical | Completed flagship and later outputs report `verification.valid=true` despite dangling process→evidence and evidence→fact IDs. | `_verify_stage` emits six constant passing checks instead of evaluating artifacts. | Pure validators now evaluate claim/source/law/graph/evidence/precedent/current-state contracts before and after review; negative-ID, source, and evidence-grounding tests pass. | closed |
| CP-003 | Critical | UI exposes an 11-node process while API supplies 19 nodes and 22 edges; inactive branches and evidence loop cannot be inspected. | Renderer iterates only `process.main_spine`; QA requires exactly 11 nodes. | The spine remains primary while non-spine nodes, branches, and the complete edge ledger are inspectable; canonical QA asserts API node/edge coverage. Browser execution remains pending. | fixed_unverified |
| CP-004 | Critical | Missing candidate/proof fields are rendered as 3 supporting reviews, 6/6 target, 12/12 protected tests, and released v4. | Success-biased frontend fallback copy in v16/v20. | Successful defaults are removed; the UI renders actual `1/3`, `not_run`, quarantined, and unchanged-v3 fields. Browser execution remains pending. | fixed_unverified |
| CP-005 | High | v18/v19/v20 overwrite release markers every 450/260/900 ms; CSS and QA observe a race. | Additive DOM-enhancement generations claim the same state. | Release identity is one static v20 marker and core render transitions emit `casepath:render`; repeated release-writing timers are absent. Browser execution remains pending. | fixed_unverified |
| CP-006 | High | Fact→evidence shows only the first source and drops passage, agent, confidence, and alternate/conflicting refs; source→facts disappears below 760 px. | Frontend projection discards canonical provenance and mobile CSS hides it. | All typed source refs render with locator, agent, confidence, state, bidirectional source/fact navigation, and compact mobile styles; canonical QA asserts exact reference parity. Browser execution remains pending. | fixed_unverified |
| CP-007 | High | Current checked-in focused QA fails against current DOM (`still-needed` vs `needed`), hard-codes causation, and may leave production state mutated after failure. | Hosted passing gate patches source at runtime; canonical source is stale and cleanup is success-only. | Canonical assertions use `needed`, derive the owning node from returned structure, clean up in `finally`, and require explicit production-mutation opt-in. Node syntax validation passes; the journey remains pending. | fixed_unverified |
| CP-008 | High | `test_pipeline.py` tests legacy `pipeline.py`; Render build never runs tests for the deployed v15 pipeline. | Test/deploy path drift. | `test_pipeline.py` now imports and exercises `pipeline_v15`, aligned API/canonicalizer tests are present, and `render-build.sh` invokes them. The 57-test backend suite passes locally. | closed |
| CP-009 | High | Live frontend/API/QA cannot prove the same source commit; release and source manifest versions are 20/15/12/10. | No common immutable release identity; stale manual manifests. | Release/deployment identity generators, API identity fields, and a QA equality preflight are implemented. Final source-manifest regeneration and same-commit post-deploy evidence remain pending. | fixed_unverified |
| CP-010 | High | Public reset deletes all visitors' runs, reviews, memories, and candidates. | One global ephemeral SQLite demo namespace. | Run/review/memory/candidate/reset operations are caller-session scoped, cross-session reads fail, and reset preserves other sessions plus the global model ledger; API isolation tests pass. Durable storage remains CP-012. | closed |
| CP-011 | High | Model-visible PDFs/emails contain generation clues and inconsistent metadata; photographs/dates conflict with narrative. | Public disclosure was embedded in evaluated artifacts; build mutation and nondeterministic metadata. | Disclosure is in the shell; evaluated bytes are deterministic, hash-bound, metadata-stripped, and mechanically scanned. Mechanical tests pass; independent blind review remains CP-014. | fixed_unverified |
| CP-012 | High | API persistence defaults to `/tmp`; no real version pointer, transaction-backed promotion, or rollback. | Resettable demo storage was treated as knowledge infrastructure. | Durable approved database, migrations, tenant scope, backups, and tested rollback. | external_dependency |
| CP-013 | High | Swiss-law operational interpretations have no qualified review; static registry lacks versioned passages/effective dates. | Reference legal mapping was presented too close to authoritative law. | Distinct official source / model interpretation / organizational rule UI and qualified Swiss-law review. | external_dependency |
| CP-014 | High | No qualified claim-process reviewers, blind realism reviewers, or lawful source-isolated transfer panel have validated the flagship. | All current reviewers and targets are generated/simulated. | Two independent qualified reviews plus adjudication and a frozen source-isolated study. | external_dependency |
| CP-015 | Medium | PDF viewer lacks extracted-text search; source tabs and document sheet have incomplete keyboard/focus semantics; `aria-busy` never clears. | Viewer accessibility and search were outside the retained mechanical gate. | Extracted-page search, WAI-ARIA tabs, dialog focus/restoration, busy-state clearing, axe checks, and keyboard assertions are implemented. Browser execution remains pending. | fixed_unverified |
| CP-016 | Medium | Hardened archive is internally intact but lacks adjacent source/evidence/pilot archives and external checksum/release sidecars; reported test counts drift. | Archive packaging and handoff files were separated. | A checksum sidecar and truthful release record now preserve the verified archive identity, exact 1,721-test clean-environment result, and unresolved source commit; the inherited non-idempotent preview rebuild remains CP-017. | closed |
| CP-017 | High | In a clean Python 3.12 environment, all 1,721 archive tests pass, but `run_all.py --validate-only --verify-manifest` rewrites reviewer-preview PNGs while rebuilding reports and then fails its own hash manifest. | Validation is not read-only or build-idempotent; report/preview generation occurs before manifest verification and emits byte-varying images. | Upstream archive source must make preview generation deterministic or verify the frozen payload before mutation, then prove a second build is byte-identical. The canonical app release records this as an inherited benchmark limitation rather than rewriting the recovered ZIP. | external_dependency |
| CP-018 | High | The first same-commit production flagship made one paid call and failed closed: Nemotron supplied a complete response with 17/18 semantic states correct, but 10 valid observable citation proposals differed from one hidden private-oracle segmentation, producing a 7:11 strict-majority failure. | Exact citation-set equality incorrectly treated passage segmentation as fact reasoning. Attempt 05 retains sanitized cost/token/identity and rejection counts; no downstream agent or final playbook was accepted. | The canonical boundary now counts only verified semantic fact contributions, deterministically projects authoritative exact sources when valid registry selections differ, and discloses projection IDs/count. Attempt 06 necessarily passed canonical merge, claim-state validation, and exact grounding before a separate provenance-persistence defect failed the run; full accepted runtime evidence remains pending. | fixed_unverified |
| CP-019 | High | The second same-commit production flagship returned a complete paid canonical response and passed canonical validation, then failed with `KeyError` before persisting contribution diagnostics or starting downstream agents. | `langchain-openrouter` does not expose `provider_name` in the synchronous `AIMessage`; complete usage and finish metadata skipped the generation lookup, while success persistence directly indexed the absent upstream-provider field. | Missing upstream-provider identity now forces a bounded metadata lookup for the same generation in both the canonical and shared specialist runners. Unavailable metadata fails with `generation_metadata_completeness` while retaining billed usage; success and failure regressions prove one inference, bounded polling, no `KeyError`, and cache-version invalidation. One aligned production retry remains required. | fixed_unverified |

## Release rule

No internally solvable critical or high defect may remain `open` at release.
Items marked `external_dependency` must stay visible and must not be restated as
implemented, verified, expert-approved, legally validated, or operationally
proven.
