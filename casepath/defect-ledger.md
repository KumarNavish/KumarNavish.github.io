# CasePath convergence defect ledger

Statuses: `open`, `in_progress`, `fixed_unverified`, `closed`, or
`external_dependency`.

| ID | Severity | Symptom and reproduction | Root cause / subsystem | Fix and regression evidence | Status |
|---|---|---|---|---|---|
| CP-001 | Critical | POST a flagship review with `decision=reject` and `building_envelope_mode=required_now`; response still approves v4 and changes the later claim to the opposite evidence state. | Review decision is recorded but ignored; promotion fields are hard-coded in `pipeline_v15.py`. | Typed request enums and decision branching now prevent rejection from creating memory/candidates or changing knowledge; aligned backend rejection and duplicate-review tests pass. | closed |
| CP-002 | Critical | Completed flagship and later outputs report `verification.valid=true` despite dangling process→evidence and evidence→fact IDs. | `_verify_stage` emits six constant passing checks instead of evaluating artifacts. | Pure validators now evaluate claim/source/law/graph/evidence/precedent/current-state contracts before and after review; negative-ID, source, and evidence-grounding tests pass. | closed |
| CP-003 | Critical | UI exposes an 11-node process while API supplies 19 nodes and 22 edges; inactive branches and evidence loop cannot be inspected. | Renderer iterates only `process.main_spine`; QA requires exactly 11 nodes. | The spine remains primary while non-spine nodes, branches, and the complete edge ledger are inspectable; canonical QA asserts API node/edge coverage. Browser execution remains pending. | fixed_unverified |
| CP-004 | Critical | Missing candidate/proof fields are rendered as 3 supporting reviews, 6/6 target, 12/12 protected tests, and released v4. | Success-biased frontend fallback copy in v16/v20. | Successful defaults are removed; the UI renders actual `1/3`, `not_run`, quarantined, and unchanged-v3 fields. Browser execution remains pending. | fixed_unverified |
| CP-005 | High | v18/v19/v20 overwrite release markers every 450/260/900 ms; CSS and QA observe a race. | Additive DOM-enhancement generations claim the same state. | Release identity is one static v20 marker and core render transitions emit `casepath:render`; repeated release-writing timers are absent. Browser execution remains pending. | fixed_unverified |
| CP-006 | High | Fact→evidence shows only the first source and drops passage, agent, confidence, and alternate/conflicting refs; source→facts disappears below 760 px. | Frontend projection discards canonical provenance and mobile CSS hides it. | All typed source refs render with locator, agent, confidence, state, bidirectional source/fact navigation, and compact mobile styles; canonical QA asserts exact reference parity. Browser execution remains pending. | fixed_unverified |
| CP-007 | High | Current checked-in focused QA fails against current DOM (`still-needed` vs `needed`), hard-codes causation, and may leave production state mutated after failure. | Hosted passing gate patches source at runtime; canonical source is stale and cleanup is success-only. | Canonical assertions use `needed`, derive the owning node from returned structure, clean up in `finally`, and require explicit production-mutation opt-in. Node syntax validation passes; the journey remains pending. | fixed_unverified |
| CP-008 | High | `test_pipeline.py` tests legacy `pipeline.py`; Render build never runs tests for the deployed v15 pipeline. | Test/deploy path drift. | `test_pipeline.py` now imports and exercises `pipeline_v15`, aligned API/canonicalizer tests are present, and `render-build.sh` invokes them. The 57-test backend suite passes locally. | closed |
| CP-009 | High | Live frontend/API/QA cannot prove the same source commit; release and source manifest versions are 20/15/12/10. | No common immutable release identity; stale manual manifests. | Release/deployment identity generators, API identity fields, and a QA equality preflight are implemented. The final 129-file source manifest is regenerated and locally verified; same-commit post-deploy evidence remains pending. | fixed_unverified |
| CP-010 | High | Public reset deletes all visitors' runs, reviews, memories, and candidates. | One global ephemeral SQLite demo namespace. | Run/review/memory/candidate/reset operations are caller-session scoped, cross-session reads fail, and reset preserves other sessions plus the global model ledger; API isolation tests pass. Durable storage remains CP-012. | closed |
| CP-011 | High | Model-visible PDFs/emails contain generation clues and inconsistent metadata; photographs/dates conflict with narrative. | Public disclosure was embedded in evaluated artifacts; build mutation and nondeterministic metadata. | Disclosure is in the shell; evaluated bytes are deterministic, hash-bound, metadata-stripped, and mechanically scanned. Mechanical tests pass; independent blind review remains CP-014. | fixed_unverified |
| CP-012 | High | API persistence defaults to `/tmp`; no real version pointer, transaction-backed promotion, or rollback. | Resettable demo storage was treated as knowledge infrastructure. | Durable approved database, migrations, tenant scope, backups, and tested rollback. | external_dependency |
| CP-013 | High | Swiss-law operational interpretations still have no qualified review. | Reference legal mapping was previously presented too close to authoritative law. | Official passage text, location/version, retrieval scope, passage/snapshot hashes, structured question joins, and deterministic-application labeling are implemented and mechanically verified. Qualified Swiss-law review remains external. | external_dependency |
| CP-014 | High | No qualified claim-process reviewers, blind realism reviewers, or lawful source-isolated transfer panel have validated the flagship. | All current reviewers and targets are generated/simulated. | Two independent qualified reviews plus adjudication and a frozen source-isolated study. | external_dependency |
| CP-015 | Medium | PDF viewer lacks extracted-text search; source tabs and document sheet have incomplete keyboard/focus semantics; `aria-busy` never clears. | Viewer accessibility and search were outside the retained mechanical gate. | Extracted-page search, WAI-ARIA tabs, dialog focus/restoration, busy-state clearing, axe checks, and keyboard assertions are implemented. Browser execution remains pending. | fixed_unverified |
| CP-016 | Medium | Hardened archive is internally intact but lacks adjacent source/evidence/pilot archives and external checksum/release sidecars; reported test counts drift. | Archive packaging and handoff files were separated. | A checksum sidecar and truthful release record now preserve the verified archive identity, exact 1,721-test clean-environment result, and unresolved source commit; the inherited non-idempotent preview rebuild remains CP-017. | closed |
| CP-017 | High | In a clean Python 3.12 environment, all 1,721 archive tests pass, but `run_all.py --validate-only --verify-manifest` rewrites reviewer-preview PNGs while rebuilding reports and then fails its own hash manifest. | Validation is not read-only or build-idempotent; report/preview generation occurs before manifest verification and emits byte-varying images. | Upstream archive source must make preview generation deterministic or verify the frozen payload before mutation, then prove a second build is byte-identical. The canonical app release records this as an inherited benchmark limitation rather than rewriting the recovered ZIP. | external_dependency |
| CP-018 | High | The first same-commit production flagship made one paid call and failed closed: Nemotron supplied a complete response with 17/18 semantic states correct, but 10 valid observable citation proposals differed from one hidden private-oracle segmentation, producing a 7:11 strict-majority failure. | Exact citation-set equality incorrectly treated passage segmentation as fact reasoning. Attempt 05 retains sanitized cost/token/identity and rejection counts; no downstream agent or final playbook was accepted. | The canonical boundary now counts only verified semantic fact contributions, deterministically projects authoritative exact sources when valid registry selections differ, and discloses projection IDs/count. Attempt 06 necessarily passed canonical merge, claim-state validation, and exact grounding before a separate provenance-persistence defect failed the run; full accepted runtime evidence remains pending. | fixed_unverified |
| CP-019 | High | The second same-commit production flagship returned a complete paid canonical response and passed canonical validation, then failed with `KeyError` before persisting contribution diagnostics or starting downstream agents. | `langchain-openrouter` does not expose `provider_name` in the synchronous `AIMessage`; complete usage and finish metadata skipped the generation lookup, while success persistence directly indexed the absent upstream-provider field. | Missing upstream-provider identity now forces a bounded metadata lookup for the same generation in both the canonical and shared specialist runners. Unavailable metadata fails with `generation_metadata_completeness` while retaining billed usage; success and failure regressions prove one inference, bounded polling, no `KeyError`, and cache-version invalidation. One aligned production retry remains required. | fixed_unverified |
| CP-020 | High | The third same-commit production flagship made one OpenRouter call and received HTTP 200, then failed at the first agent with SDK `ResponseValidationError`; the ledger retained neither response identity nor usage and summarized absent actual cost as zero. | `openrouter` 0.11.46 validates the HTTP 200 JSON body as `ChatResult` before the LangChain adapter can return its raw envelope; its generated model also treats the nullable `system_fingerprint` field as required, so a compatible provider response can fail before the existing safe provenance path. | The shared LangChain client now delegates exactly once, catches only that SDK validation error, transiently validates a bounded minimal completion envelope, strips nonconsumed provider fields, and returns the known fields to LangChain. Invalid, oversized, duplicate-key, non-finite, or deeply nested bodies fail under a constant `provider_response_envelope` invariant with no raw exception chain. Pinned-SDK, LangChain structured-output, billing, fuzz, and canonical/specialist regressions pass; one aligned production retry remains required. Attempt 07 stays unaccepted and its USD 0.027645 estimate is not actual cost. | fixed_unverified |
| CP-021 | High | Attempt 08 made exactly one paid canonical-facts call, retained response identity and complete usage, then failed `generation_metadata_completeness` before any downstream receipt. A later read-only lookup for the same response returned the dated model, DeepInfra, and identical usage. | OpenRouter's same-generation metadata was eventually consistent beyond the deployed bounded lookup window; the application required upstream-provider provenance before accepting the model contribution and therefore failed closed too early. | The bounded same-generation lookup now performs at most eight reads with capped backoff, retains poll count/latency and synchronous billing on exhaustion, performs no inference retry, and is covered on canonical and specialist paths. Attempt 08 remains rejected history; one aligned production journey is still required. | fixed_unverified |
| CP-022 | High | Attempt 09 made one CasePath canonical-facts network call and failed `provider_response_envelope`; the signed-in OpenRouter Upstream Requests view later showed two router-level upstream attempts ending at Together with status 400, while the exact-ID generation lookup returned 404. | Default provider routing selected Together and ended with upstream 400 after two router attempts, while the prior DeepInfra request returned 200. The exact internal provider error message remains unknown; no completed generation, application usage, or actual charge was established. | Source now pins `deepinfra/fp4`, disables provider fallback, requires parameter support, denies provider data collection, enables bounded generation metadata, and gates successful provenance on `DeepInfra`. Exact one-send/header/body/error/billing tests pass locally. Attempt 09 remains rejected and its USD 0.027645 estimate is excluded from known charges; an aligned production journey is still required. | fixed_unverified |
| CP-023 | High | Attempts 10 and 11 each made two DeepInfra calls: canonical facts completed with disclosed guarded fallback, then the Nemotron orchestrator returned `length` at the configured ceiling—first 400, then exactly 800 completion tokens. Both runs failed closed before any specialist or deterministic gate; attempt 11 cost USD 0.0286577. | The orchestrator's strict structured plan exceeded even the expanded 800-token output budget; raising the ceiling from 400 to 800 did not resolve the boundary. | The bounded plan surface was reduced. In attempt 12 the orchestrator completed with `stop`, one accepted item, no rejected or ignored proposal, no fallback, 527 total tokens, and USD 0.0003892 cost; the downstream parallel specialist fan-out then started. Attempts 10 and 11 remain rejected history, while the later process-mapping failure is tracked separately as CP-024. | closed |
| CP-024 | High | Attempt 12 reached the parallel specialist fan-out: document/source integrity completed without fallback, but process-decision mapping returned a complete 22,099-token `stop` response and failed `model_contribution_majority`. The four-call run cost USD 0.0332561 and stopped before any deterministic gate or later model role. | The old process specialist had to rediscover hidden exact fact/state/value/source tuples from a 356-reference registry instead of contributing a bounded causal mapping. | Process now receives canonical handoffs and contributes only bounded decision values; evidence and final roles score independent model-owned fields, deterministic gates causally rematerialize returned DTOs, safe failure diagnostics persist, and the six-role/three-gate contract is locally verified. Attempt 12 remains rejected history; one aligned production journey is still required. | fixed_unverified |
| CP-025 | High | Attempt 13 made one canonical-facts request and failed closed with `TooManyRequestsResponseError` before any accepted model result, downstream role, or deterministic gate. The OpenRouter receipt shows one DeepInfra attempt ending in 429; the application ledger retained no identity, usage, or actual cost. | External DeepInfra returned or was attributed the 429 through OpenRouter. The provider-internal cause is unknown. Healthy account credits and only 0.6536316% use of the USD 25 key limit rule out key/account hard-limit exhaustion. | Keep attempt 13 rejected and its unknown cost excluded. Re-establish upstream availability, then run one explicitly authorized aligned cold journey; do not weaken routing, retry, or acceptance gates. | external_dependency |
| CP-026 | High | The focused QA gate could never accept the current release contract because it omitted the required grounded-causal-recomputation and learning-replay criteria. | The browser gate's exact runtime-acceptance projection drifted behind `release.json`. | The two criteria now remain exact in QA and a release regression pins them. | closed |
| CP-027 | High | Three later-claim files entered the observable model package while the release artifact manifest marked them non-model-visible and skipped leakage scanning. | The release verifier's primary artifact allowlist predated the expanded six-file later claim. | All twelve observable claim files are model-visible; a regression imports the authoritative claim catalog and proves every observable filename receives a passed leakage scan. | closed |
| CP-028 | High | The browser computed the held-out claim's no-memory baseline before the simulated review, so the supposedly later comparison was already analyzed during memory construction. | Comparison convenience violated the temporal holdout boundary. | Baseline construction now begins only after review and memory consolidation, immediately before the memory-enabled run; QA pins the post-learning holdout disclosure. | closed |
| CP-029 | High | Candidate `protected_regression` tested only nine negative eligibility dictionaries and did not bind an actual playbook/checklist output. | Governance conflated semantic routing checks with protected output regression. | The protected report now includes a source-claim control bound to exact pre-review result/process/checklist hashes and proves memory ineligibility leaves all three unchanged; backend and browser gates assert it. | closed |
| CP-030 | High | The flagship has no qualified expert review, qualified Swiss-law approval, blind realism review, durable organizational persistence, or real-claim authorization. | These require independent people, institutional controls, and infrastructure beyond the generated-demo workspace. | Keep all corresponding release truth fields false, label the correction and memory unverified, keep shared v3 unchanged, and obtain the external evidence before any real-claim or expert-approved claim. | external_dependency |
| CP-031 | High | Same-commit QA deploy `dep-d9u0fept0dsc73c90sag` failed before inference because the later-claim renderer did not retain the exact `qualified_expert_reviewed` provenance branch required by the loaded-source contract. The public model ledger remained at zero calls and zero cost. | The later reuse renderer recognized only `unverified_demo_memory`, while the precedent renderer and QA contract distinguish qualified and unverified review authority. | The later renderer now accepts both exact review statuses, keeps authority-specific copy, and a local release regression validates both loaded renderers before deployment. | closed |
| CP-032 | High | Attempt 14's logical specialist fan-out created the process and document/source provider calls 4.051 ms apart; both later failed at the OpenRouter boundary with 429. The failed calls retained only the expected DeepInfra route, not their actual upstream identity. | The DAG correctly exposed two specialist branches, but physical provider sends had no process-wide concurrency admission cap. The timing proves overlap was possible; it does not establish that overlap caused either 429, whose provider-internal cause remains unknown. | Preserve `parallel_groups` as logical fan-out/join topology, but admit OpenRouter sends through a process-wide `provider_max_in_flight: 1` gate. Admission timeout is a zero-call `blocked_provider_concurrency` failure with no response, usage, actual cost, or cacheable output; static/dynamic release pins and positive/tamper tests enforce it. At the Attempt 14/source-freeze observation, same-commit production acceptance remained pending; a later authoritative dynamic QA artifact pair may supersede that historical verdict. CP-025 retains the external provider-availability boundary. | fixed_unverified |
| CP-033 | High | Attempt 15 accepted all six model roles and all three deterministic gates, but focused QA saw `whole-playbook-validator/15.2` as a fourth gate identity and failed the exact visible-gate check. | The orchestration renderer used `validator` and `label` as fallbacks for `gate_id`, conflating a terminal deterministic validator receipt with a release-owned deterministic gate. | Gate identity now comes only from `gate_id` or `agent_id`; a validator remains visibly labeled as a validator without receiving `data-gate-id`. Loaded-source, release, and rendered-browser regressions pin the distinction. Attempt 15 remains failed-closed history; at this source freeze the repair was pending a new same-commit production gate, whose later authoritative dynamic evidence may verify it without rewriting this history. | fixed_unverified |

## Release rule

No internally solvable critical or high defect may remain `open` at release.
Items marked `external_dependency` must stay visible and must not be restated as
implemented, verified, expert-approved, legally validated, or operationally
proven.
