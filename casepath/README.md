# CasePath frontend

Static frontend for the focused CasePath v20 demonstration (release contract 2.2; API/pipeline 15.2). The browser reads original generated source artifacts from the API and keeps each source file separate from the returned extraction and deterministic annotations.

Production API default: `https://casepath-agentic-api.onrender.com`
