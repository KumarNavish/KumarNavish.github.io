# Motion-native research instrument

This branch replaces the passive FIELD vertical slice with six computation-backed interactive research chapters:

1. Signed graph Laplacians, eigendecomposition, eigenmodes, and heat diffusion.
2. Experience Replay / GBR with exact, greedy, and random subset matching.
3. Rank Feasibility with constrained minimum-norm correction solving.
4. TiC-LM fixed-budget temporal replay allocation and regret-row simulation.
5. CasePath fail-closed evidence and deterministic-gate workflow.
6. Generative AI × spatial computing through a deterministic intent-to-world compiler.

SafePatch is removed from source, routes, package scripts, build commands, and deployable public assets.

Every chapter follows question → mechanism → intervention → consequence → evidence → boundary → continuation, with real controls and explicit scientific scope.
